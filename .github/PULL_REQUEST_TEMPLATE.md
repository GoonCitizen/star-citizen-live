## Summary
<!-- What and why. Keep it small. See DEVELOPERS.md. -->

## Test plan
- [ ] `npm test`
- [ ] Parser change includes a real `Game.log` line and game version (do not invent CIG formats)
- [ ] No secrets (`settings/local.js`, tokens, seeds, webhooks)

Base branch: `feature/rsi`. Do not merge to `master`.
