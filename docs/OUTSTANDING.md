# Outstanding (security-first)
Living queue for this repo. Detail: [SECURITY.md](../SECURITY.md), [THREAT-MODEL.md](THREAT-MODEL.md). Suite march: [@fabric/core `docs/PRODUCTION_MARCH.md`](https://github.com/FabricLabs/fabric/blob/feature/rsi/docs/PRODUCTION_MARCH.md).

**Last reviewed:** 2026-08-14. Pins: core `5557a2bf` ([#185](https://github.com/FabricLabs/fabric/pull/185)), http `7536341` ([#69](https://github.com/FabricLabs/fabric-http/pull/69)), hub `361a750` ([#16](https://github.com/FabricLabs/hub.fabric.pub/pull/16)), discord `175d9f2` ([#2](https://github.com/FabricLabs/fabric-discord/pull/2)).

## Blockers before LAN / hosted dashboard claims
1. ~~**`httpSharedMode` mutating APIs**~~ — non-loopback writes now need Schnorr/Bearer (`functions/httpRemoteAuth.js`), same bar as `SC_MODE=server`. Loopback still uses the unlocked identity **on desktop**. Public relays **must** use `SC_MODE=server` because Caddy → loopback would otherwise inherit that unlocked path (`docs/PRODUCTION.md`). Unauth **GET** of settings / guilds / peers on a shared bind remains an information leak.
2. **Chat confidentiality** — global chat and DirectChat are signed plaintext vs relays ([THREAT-MODEL.md](THREAT-MODEL.md)). GroupChat v2 participant seal is hub-blind; v1 tip-HKDF is not.
3. **Identity cluster** — a stolen device key **is** the actor until `IdentityCrossSignRevoke`. Union-find is `@fabric/hub/functions/identityCluster` via the local re-export.

## Next slices
- [ ] DirectChat E2E or `P2P_FORWARD` (pair ARC + participant seal).
- [ ] Drop GroupChat seal v1 once the mesh no longer emits it.
- [ ] Bearer revoke + shorter TTL for hosted sessions.
- [ ] Bind device-link `sessionId` into attest messages with http/Passport (suite redeem possession proof).
- [ ] Mesh chat flood / anonymous inbound rate limits.
- [ ] Shared-bind GET privacy (settings / Discord catalog / peers) without breaking the LAN dashboard.
- [ ] Playnet Hub heap OOM still cycling after Hub `361a750` ([tick 1/8](https://relay.goon.vc/downstream.agents.md): 248 restarts). Core `5557a2bf` numeric-98 dispatch + Hub Peer debug gate did **not** stop it. Hub no longer forces Bitcoin RPC debug (local Hub tree, not this pin yet); http `7536341` `collectOwnFabricHosts` no longer treats sibling NIC `.166` as RSI self (Hub↔RSI ESTAB was missing). Live 2026-08-14: mint succeeded (`operator-master`); production Hub still returned `adminToken required` (Hub `_rootKey` is not this operator key; pending GoonCitizen remains first-writer `15995567…` from 2026-08-12). Deploy Hub with the same `FABRIC_XPRV` (or the operator-admin token helper) before `--accept` can land.

## Closed / already in tree (do not regress)
- Loopback HTTP default; D-017 log share off; allowlisted Hub login/link; IdentityCrossSign gossip after device-link; Android local-first node. Dashboard keeps a browser-safe local IdentityCrossSign copy (do not webpack `@fabric/core`); `signCrossSign` binds `localPubkey` to `fabricKey.pubkey` and rejects unknown `kind` (same as [@fabric/core #185](https://github.com/FabricLabs/fabric/pull/185)).
- Stale Fabric `:7778` hub dials rewritten to `:7777` (DNS **and** dedicated NICs `65.21.231.166` / `65.21.231.149`); self IPs dropped; peer-dial `ECONNREFUSED` logs rate-limited. A unicast `FABRIC_INTERFACE` does not treat sibling NICs as self (Hub `.166` vs RSI `.149` on meta.fabric.pub). `FabricNetwork` also wraps core `Peer._connect` so `Peer.start` / `_fillPeerSlots` cannot TCP-dial `.149:7778` from peersDb. Do not stash-pop the old GoonCitizen `fabric 7777` stash onto `fabricRelayBoot`. This core pin (`5557a2bf`) includes `fabricHomeEnv` / `fabricKeyMaterial`, numeric `P2P_PEERING_OFFER` dispatch, UTF-8 shoutbox `fabricChatText`, IPv6 `_connect` bracket strip, first-tier RC1 contract, undersize AMP drop, candidate cooldown / NOISE-after-connect / in-flight `_outboundDialTargets`, and canonical `host:port` dial keys. Local IdentityCrossSign copy matches core `_normPubkey` (no `:` field smash). APK `fabricPeerHostLocal` strips `pubkey@` userinfo; http pin `7536341` does the same in `@fabric/http/functions/fabricPeerHost` and filters sibling NICs (GoonCitizen prefers that module, local fallback for the APK).
- Fabric pins refreshed to GitHub `feature/rsi` tips above (Hub `361a750` includes `./functions/bulkSecurityAdvisory` + `operatorAdminToken` exports, Peer debug gate, UTF-8 WebRTC shoutbox unpack). `identityCluster` re-exports Hub; `normalizeDiscordSettings` re-exports `@fabric/discord`. Discord `175d9f2` fails the OAuth authorize stub closed (501) and debounces voice flag `commit()`; GoonCitizen does not mount `/services/discord/authorize`. Device-link fetch uses http `deviceLinkHeaders` (browser omits client-set Origin/Referer). Http `fabricChatNormalize` re-exports core `fabricChatText`. Local Files catalog drops GHSA / OpenSSF bulk-advisory payloads via Hub `looksLikeBulkSecurityAdvisory`.
- `FABRIC_SEED` raw hex restore + `~/.fabric/hub-admin-token` / `~/.fabric/env` via core `fabricHomeEnv` / `fabricKeyMaterial` / `fabricWalletIdentity` on pin `5557a2bf` (local wrappers still try/catch for older `npm link`).
- Shared shoutbox ingest via `@fabric/http/functions/fabricChatNormalize` (`functions/fabricChatNormalize.js` re-export). Global mesh remains UTF-8 `P2P_CHAT_MESSAGE`; Store still `{ channel, body, handle }`.
- Shared-mode LAN **writes** require a session (`shouldEnforceRemoteAuth`).

## PRs
[#7](https://github.com/GoonCitizen/star-citizen-live/pull/7) — open **WIP: Android** (`feature/rsi` → `master`). No review comments yet; GitHub reports no CI checks on the branch. Historical [PR #6](https://github.com/GoonCitizen/star-citizen-live/pull/6) is closed overlay/replay. Do not merge to `master`. Local `npm run test:unit` after Hub `361a750` / http `7536341` / core `5557a2bf` / discord `175d9f2`: **265 passing**.
