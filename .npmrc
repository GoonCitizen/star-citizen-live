# npm 12+ defaults allow-git=none.
# Root pins @fabric/core, @fabric/http, and @fabric/hub from GitHub. Hub/http
# also depend on core/http as git; nested `npm install` (git dep preparation)
# resolves those to commit SHAs. `allow-git=root` refuses that fetch — use `all`.
allow-git=all
