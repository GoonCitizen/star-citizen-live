'use strict';

/**
 * Re-export from `@fabric/discord` (pin ≥ `175d9f2`). Kept as a stable local
 * path for older requires / docs; prefer the package export in new code.
 */
module.exports = require('@fabric/discord/functions/normalizeDiscordSettings');
