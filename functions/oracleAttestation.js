'use strict';

/**
 * OracleAttestation envelope helpers — canonical in `@fabric/http`.
 * GoonCitizen keeps this path for historical imports; do not fork the body.
 */
module.exports = require('@fabric/http/functions/oracleAttestation');
